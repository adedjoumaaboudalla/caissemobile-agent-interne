<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CaisseMobile" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="windowNoTitle">true</item>

        <item name="windowActionBar">false</item>

        <item name="colorPrimary">@color/my_primary</item>

        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/my_primary_dark</item>

        <!--   theme UI controls like checkboxes and text fields e.g. FloatActionButton -->
        <item name="colorAccent">@color/my_accent</item>

        <item name="android:textColorHighlight">@color/my_primary_light</item>


        <item name="android:windowBackground">@color/window_background</item>

    </style>

    <!--
       Base application theme, dependent on API level. This theme is replaced
       by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
   -->
    <style name="AppBaseTheme" parent="Base.Theme.CaisseMobile">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:bottomDark">@color/my_primary_dark</item>
    </style>

    <style name="ExpandedAppBar">
        <item name="android:textSize">42sp</item>
        <item name="android:textStyle">bold</item>
        <!-- <item name="android:src">@mipmap/avatar</item> -->
         <item name="android:textColor">@color/my_primary_text</item>
     </style>
     <style name="ColapseAppBar">
         <item name="android:textSize">24sp</item>
         <item name="android:textColor">@color/my_primary_text</item>
     </style>

    <style name="SingleGradient">
        <item name="android:background">@drawable/selector_gradient</item>
        <item name="android:padding">@dimen/button_padding</item>
        <item name="android:textColor">@drawable/selector_color</item>
    </style>

    <dimen name="button_padding">5dp</dimen>
</resources>
